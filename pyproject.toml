[project]
name = "investment-analysis-template"
version = "0.1.0"
description = "投資分析を一定の前提で進めるための最小テンプレート"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = []
